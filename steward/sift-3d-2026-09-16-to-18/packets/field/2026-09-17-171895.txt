# Grok Bot update_state memory writes fail fleet-wide: brain docs snapshot was cut short before completing
URL: https://forum.cursor.com/t/grok-bot-update-state-memory-writes-fail-fleet-wide-brain-docs-snapshot-was-cut-short-before-completing/171895
Created: 2026-09-16T15:07:36.348Z

## USER @doughy · 2026-09-16T15:07:36.382Z · post#1
Where does the bug appear (feature/product)? Grok Bot Describe the Bug Summary update_state with target: memory fails on every bot I tested with the exact error: brain docs snapshot was cut short before completing Settings updates via update_state still work. Direct file writes to memory/profile.md and memory/log/*.md still work. Failed memory writes do not reliably land the fact. Where it happens Recreated / reimported bots (e.g. after sidebar Delete + reimport): CreateAgent often leaves incomplete agent-data — missing memory/ folder, no conversation-blobs.db, empty latestRootBlobId. Scaffolding memory/ alone does not fix update_state. Long-lived bots too same error. So this is not recreate-only and not explained by one oversized chat. Repro Open any bot (fresh or old). Ask it to run update_state → target: memory, action: write, short log/note fact. Observe tool error: brain docs snapshot was cut short before completing. Confirm the fact is absent from memory/*.md (unless the bot file-appended as a workaround). Optionally: update_state → target: settings still succeeds on the same bot. Expected Memory write succeeds; fact appears in agent memory / prompt snapshots. Actual Tool errors; brain-docs snapshot aborts; store prompt snapshots can lag disk; bots fall back to editing memory/*.md by hand. Notes from local investigation SQLite stores integrity-check OK; disk not full; agent-data → sand-data symlink fine. Host shows related brain-docs diagnostics (brain_docs_compensation_failed / brain_docs_channel_config_corrupt). Box host stamp observed: 332d021b. Example agent ids: CoS f283b3c0-75c5-41ac-a60b-455ef252740e; also reproduced on Economist and a brand-new Engineer. Already filed in-app feedback (privacy mode blocked SendFeedback from the bot; filed via Help → Send Feedback). Workaround File-append durable facts to memory/profile.md / memory/log/YYYY-MM.md until fixed. Do not recommend Update/Reset Computer as the first fix for this (looks harness/brain-docs side). Steps to Reproduce Open any bot (fresh or old). Ask it to run update_state → target: memory, action: write, short log/note fact. Observe tool error: brain docs snapshot was cut short before completing. Confirm the fact is absent from memory/*.md (unless the bot file-appended as a workaround). Optionally: update_state → target: settings still succeeds on the same bot. Expected Behavior Memory write succeeds; fact appears in agent memory / prompt snapshots. Operating System MacOS Version Information Version: 0.53.0 Release Track: stable OS: darwin Does this stop you from using Cursor No - Cursor works, but with this issue

## STAFF @deanrie · 2026-09-16T16:04:25.114Z · post#5
Hey, thanks for the detailed report. It really helps that you included the exact error, steps, and what you observed right away. This is a known issue we’re already tracking. It shows up exactly the way you described: memory writes via update_state (target: memory) fail with brain docs snapshot was cut short before completing , while update_state with target: settings and direct file edits work fine. The fact that it happens on both new and old bots is expected for this issue, so it’s not caused by a single overloaded chat or anything in your setup. Your workaround is spot on. Have the bot write important facts directly into memory/profile.md or memory/log/YYYY-MM.md until this is fixed. You don’t need to run Update / Reset Computer for this, it won’t change anything. I can’t share a timeline yet, but I’ll post in this thread as soon as there’s an update.
