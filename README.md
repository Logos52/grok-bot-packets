# grok-bot-packets

Files the Grok Bot fleet writes under `/workspace` on the shared cloud computer, pushed here on weekdays by the Steward bot. Public material only. `/workspace/secrets/` is never committed.

Read on the Mac by the `/fold` skill: pull, list files newer than the last fold, decide what to keep.

Layout mirrors `/workspace`: `watch/`, `field/`, `recap/`, `brief/`, `yuedu/`, `table/`, `corpus/`, `steward/`.
